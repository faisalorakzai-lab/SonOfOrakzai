{
  "buildCommand": "corepack enable && corepack prepare pnpm@10.33.3 --activate && pnpm install --frozen-lockfile=false && cd artifacts/son-of-orakzai && BASE_PATH=/ NODE_ENV=production pnpm run build",
  "outputDirectory": "artifacts/son-of-orakzai/dist/public",
  "framework": null,
  "installCommand": "echo skip",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
